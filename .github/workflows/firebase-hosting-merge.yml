# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - dev
permissions:
  id-token: write
  contents: read
  security-events: write
  checks: write
  pull-requests: write
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sed -i "s/\${API_URL}/${API_URL}/g" src/environments/environment.pipeline.ts
      - run: npm ci && npm run gql-generate && npm run build --configuration pipeline
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          node-version: 20
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_RECIPES_91982 }}'
          channelId: live
          projectId: recipes-91982
