# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - dev
permissions:
  id-token: write
  contents: read
  security-events: write
  checks: write
  pull-requests: write
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
#      - run: env API_URL=${API_URL} FB_PROJECT_ID=${FB_PROJECT_ID} FB_APP_ID=${FB_APP_ID} FB_DATABASE_URL=${FB_DATABASE_URL} FB_STORAGE_BUCKET=${FB_STORAGE_BUCKET} FB_LOCATION_ID=${FB_LOCATION_ID} FB_API_KEY=${FB_API_KEY} FB_AUTH_DOMAIN=${FB_AUTH_DOMAIN} FB_MESSAGING_SENDER_ID=${FB_MESSAGING_SENDER_ID} FB_MEASUREMENT_ID=${FB_MEASUREMENT_ID} sed -i "s/\${\(API_URL\|FB_PROJECT_ID\|FB_APP_ID\|FB_DATABASE_URL\|FB_STORAGE_BUCKET\|FB_LOCATION_ID\|FB_API_KEY\|FB_AUTH_DOMAIN\|FB_MESSAGING_SENDER_ID\|FB_MEASUREMENT_ID\)}/\${\1}/g" src/environments/environment.pipeline.ts
#      - run: sed -i "s/API_URL/${{ secrets.API_URL }}/g" src/environments/environment.pipeline.ts
#      - run: sed -i "s/API_URL/$API_URL/g" src/environments/environment.pipeline.ts
      - run: sed -i "s#API_URL#${{ secrets.API_URL }}#g" src/environments/environment.pipeline.ts
      - run: cp src/environments/environment.pipeline.ts src/environments/environment.ts
      - run: cp src/environments/environment.pipeline.ts src/environments/environment.prod.ts

      - run: echo "Environment variables:"
      - run: env
      - run: echo "Environment variables:"
      - run: echo "API_URL=$(API_URL)"
      - run: echo "FB_PROJECT_ID=$(FB_PROJECT_ID)"
      - run: echo "Environment.ts file:"
      - run: cat src/environments/environment.ts
      - run: echo "${{ secrets.API_URL }}"

      - run: npm ci && npm run gql-generate && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          node-version: 20
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_RECIPES_91982 }}'
          channelId: live
          projectId: recipes-91982
