# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
permissions:
  id-token: write
  contents: read
  security-events: write
  checks: write
  pull-requests: write
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sed -i "s/\${\(API_URL\|FB_PROJECT_ID\|FB_APP_ID\|FB_DATABASE_URL\|FB_STORAGE_BUCKET\|FB_LOCATION_ID\|FB_API_KEY\|FB_AUTH_DOMAIN\|FB_MESSAGING_SENDER_ID\|FB_MEASUREMENT_ID\)}/\${\1}/g" src/environments/environment.pipeline.ts
      - run: cp src/environments/environment.pipeline.ts src/environments/environment.ts
      - run: npm ci && npm run gql-generate && npm run build --configuration pipeline
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          node-version: 20
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_RECIPES_91982 }}'
          projectId: recipes-91982
